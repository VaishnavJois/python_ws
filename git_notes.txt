git init

git add .

git config --global user.email "vaishnavr09@gmail.com"

git config --global user.name "VaishnavJois"

git commit -m "day2"

git remote add origin https://github.com/VaishnavJois/python_ws.git

git push -u origin master